Fix getBusinessConfig / upsertBusinessConfig to use tenant-aware DB. Apply these three changes exactly. Do not refactor anything outside the specified files.
In server/storage.ts, find getBusinessConfig and upsertBusinessConfig. Add an optional schema?: string parameter to both. At the top of the file import getTenantDb if not already there: import { getTenantDb } from './db-tenant'; In each function, add const dbInstance = schema ? getTenantDb(schema) : db; at the top and replace every use of db with dbInstance throughout that function.
In server/routes.ts, find every call to storage.getBusinessConfig() and storage.upsertBusinessConfig() in the entire file and add req.tenantSchema as the last argument to each call. Then replace the GET /api/admin/business-config handler body with a try-catch that calls storage.getBusinessConfig(req.tenantSchema), returns the config or a fallback empty object, and on catch does console.error('[business-config GET]', error) and returns res.status(500).json({ message: 'Error al cargar la configuración del negocio' }). Do the same for the PUT handler calling storage.upsertBusinessConfig(req.body, req.tenantSchema), with console.error('[business-config PUT]', error) on catch.
In client/src/pages/admin/business-config.tsx, destructure error and isError from the useQuery call that fetches /api/admin/business-config. Make sure useQueryClient is imported and const queryClient = useQueryClient() is called. After the loading check and before the form JSX, add an if (isError) block that returns a div with class "p-4" containing a div with classes "bg-destructive/15 border border-destructive text-destructive rounded-md p-4 flex flex-col gap-2" showing a paragraph with text "Error al cargar la configuración", a paragraph with (error as Error)?.message or 'Error desconocido', and a button with onClick that calls queryClient.invalidateQueries with queryKey ['/api/admin/business-config'] and text "Reintentar".
Acceptance criteria: after deploy, Admin → Configuración de Empresa shows the saved values including businessName "Restaurante y Granja La Antigua Lecheria". If the endpoint fails, user sees an error banner with a Reintentar button instead of empty fields. Server logs show [business-config GET] prefix on any error.